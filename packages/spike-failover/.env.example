# Produced by: terraform -chdir=infra/spike output -raw spike_env > packages/spike-failover/.env
DSQL_ENDPOINT_USE1=
DSQL_ENDPOINT_USE2=
DSQL_REGION_USE1=us-east-1
DSQL_REGION_USE2=us-east-2

# Optional: force region(s) unreachable at startup for a manual demo (comma-separated).
DSQL_UNREACHABLE=
# Optional: event count for the active-active claim (default 50).
SPIKE_EVENTS=
